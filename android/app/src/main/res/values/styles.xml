<resources>
    <!-- Dark is the default look; MainActivity switches the bars to light when the app is set to light. -->
    <style name="CadenceTheme" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">#1F1B19</item>
        <item name="android:navigationBarColor">#2C2826</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:windowActionModeOverlay">true</item>
    </style>
</resources>
